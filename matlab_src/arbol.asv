fprintf("PROGRAM MAAAAAAN");

fileID = fopen("post-operative.data");
uiimport("post-operative.data")
m = width(postoperative);
X = postoperative(:, m);
Classes = unique(X);

for i = 1:m-1
    AttrMatrix = postoperative(:, i);
    ValuesAttr = unique(AttrMatrix);
    
end